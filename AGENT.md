# Agent Instructions

이 저장소에서 코드를 수정하는 모든 Agent는 다음 규칙을 최우선으로 따른다.

- 모든 수정 후에는 `pytest`로 전체 테스트 케이스가 통과하는지 반드시 확인한다.
- 수정 사항은 의미 있는 Atomic commit 단위로 나누어 Git에 반영한다.
- 모든 수정 시 `pydantic`을 활용해 변수의 역할, 타입, 제약 조건, 기본값 등 속성이 명확히 드러나도록 모델링한다.
